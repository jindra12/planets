import { Dynamic } from "../Dynamic";
import { AppHead } from "./_app-head";

const NotFound = () => {
    return (
        <>
            <AppHead />
            <Dynamic />
        </>
    );
};

export default NotFound;
