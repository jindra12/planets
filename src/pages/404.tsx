import { Main } from "..";
import { AppHead } from "./_app-head";

const NotFound = () => {
    return (
        <>
            <AppHead />
            <Main />
        </>
    );
};

export default NotFound;
