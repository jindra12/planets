import { Main } from "../..";
import { AppHead } from "../_app-head";

const Person = () => {
    return (
        <>
            <AppHead />
            <Main />
        </>
    );
};

export default Person;
